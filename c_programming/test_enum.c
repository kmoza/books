#include<stdio.h>

enum {ON,OFF};

int main()
{
	//test passing enum to functions
	/*
	switch on = ON;

	if(on == ON)
	{
		printf("The switch is on\n");
	}
	else
		printf("The switch is off\n");

	*/
	return 0;
}
